import { StyleSheet } from "react-native";


export const styles = StyleSheet.create({
    title: {
        marginTop: 15,
        marginBottom:15,
        fontSize: 14,
        color: "#212121",
        fontWeight: "bold",
        marginLeft:6,
      },
});